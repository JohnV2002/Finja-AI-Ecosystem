# fake-omni

## Error Contract

This project uses structured errors **FINJA-xxx**. Read `ERROR_CONTRACT.md`. Never use print/console.log as the error path. Engine: `python -m error_contract` (plugin: finja - exception Plugins).
